syntax = "proto3"; // 指定proto版本

package common;     // 指定包名

// import "google/protobuf/descriptor.proto";

// option go_package = "common;common"; // 指定路径
// option go_package = "gmsec/rpc/common";
option go_package = "rpc/common"; // 指定包路径

message ResultResp {
	// bool status = 1;
	string msg = 1;
}

message Empty {
	
}


// message SimpSharInfo {
// 	string code = 1;// 当前代码
// 	string name = 2;// 当前名字
//     double price = 3;// 当前指数
//     double percent = 4;// 当前涨跌比例
//     string color = 5;// 颜色
// 	string url = 6;// 地址 
// }

message SimpSharInfo {
	string code = 1;// 当前代码
	string name = 2;// 当前名字
    double price = 3;// 当前指数
    double percent = 4;// 当前涨跌比例
    string color = 5;// 颜色
	string url = 6;// 地址 
	SimpSharInfo item = 7;// 子项
}